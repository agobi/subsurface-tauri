// AI-generated (Claude)
import { mount } from "svelte";
import "./lib/styles/tokens.css";
import ToolbarHarness from "./ToolbarHarness.svelte";

const app = mount(ToolbarHarness, {
  target: document.getElementById("app")!,
});

export default app;
