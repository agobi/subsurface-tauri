// AI-generated (Claude)
import { mount } from "svelte";
import "./lib/styles/tokens.css";
import DcDownloadHarness from "./DcDownloadHarness.svelte";

const app = mount(DcDownloadHarness, {
  target: document.getElementById("app")!,
});

export default app;
