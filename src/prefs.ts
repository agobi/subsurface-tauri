// AI-generated (Claude)
import { mount } from "svelte";
import "./lib/styles/tokens.css";
import Prefs from "./Prefs.svelte";

const app = mount(Prefs, {
  target: document.getElementById("app")!,
});

export default app;
